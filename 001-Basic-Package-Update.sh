# Update the repository
sudo apt update
# Update packages without asking
sudo apt -y upgrade

# Essential tools
sudo apt -y install git-core wget curl

# Search
# ripgrep: https://github.com/BurntSushi/ripgrep
sudo apt -y install ripgrep tree

